using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class collectibleScript : MonoBehaviour
{
    // Start is called before the first frame update
    public int hoverInt = 20;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
